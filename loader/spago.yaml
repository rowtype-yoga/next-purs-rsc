package:
  name: route-loader
  dependencies:
    - language-cst-parser
    - prelude
    - effect
    - console
    - arrays
    - strings
    - maybe
    - foldable-traversable
    - tuples
    - ordered-collections
  build:
    strict: true
  test:
    main: Test.Loader
    dependencies:
      - assert
